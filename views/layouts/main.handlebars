<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title> Treasure Hunter's </title>
  <link rel="stylesheet" href="/stylesheets/style.css">
</head>



<header>
  <div class="header">
    <nav class="navigation">
      <a href="/" class="navbar-logo"> Treasure Hunter's </a>
      <div class="navbar-right">
        {{#if loggedIn}}
        <a href="/dashboard"> Dashboard </a>
        <button id="logout" > Logout </button>
        {{else}}
        <a href="/login"> Login </a>
        {{/if}}
      </div>
    </nav>
</header>

<body>
  <main>
      {{{ body }}}
    </main>
    {{#if loggedIn}}
      <script src="/javascript/logout.js"></script>
      {{/if}}
</body>

 {{!-- <body>
  <div class="wrapper">
    <header>
      <h1>
        <a href="/"> Treasure Hunter's </a>
      </h1>
      <nav>
        {{#if loggedIn}}
        <a href = "/dashboard"> Dashboard </a>
        <button id="logout" class="btn-no-style"> Logout </button>
        {{else}}
        <a href="/login"> Login </a>
        {{/if}}
      </nav>
    </header>
    <main>
      {{{ body }}}
    </main>
    <footer>
      Thanks for visiting!
    </footer>
  </div>
      {{#if loggedIn}}
      <script src="/javascript/logout.js"></script>
      {{/if}}
</body>  --}}
</html>
